# Prevent strict peer dependency errors during install
strict-peer-dependencies=false
auto-install-peers=true

# Allow workspace packages to be resolved correctly
link-workspace-packages=true

# Use consistent store location
prefer-frozen-lockfile=false
